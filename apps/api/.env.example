DATABASE_URL="postgresql://user:password@localhost:5432/amp_csr_portal?schema=public"

# Shared with the gateway Worker, which sends it as X-Gateway-Secret. Leave
# unset locally: outside production an unset secret disables the check.
# GATEWAY_SECRET=
