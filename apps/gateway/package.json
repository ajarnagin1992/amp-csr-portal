{
  "name": "@amp-csr/gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --var API_ORIGIN:http://localhost:3000",
	"lint": "oxlint src/ test/",
    "start": "wrangler dev --var API_ORIGIN:http://localhost:3000",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/tsconfig.json",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "@types/node": "^24.13.5",
    "typescript": "^6",
    "vitest": "~4.1.0",
    "wrangler": "^4.132.0"
  }
}